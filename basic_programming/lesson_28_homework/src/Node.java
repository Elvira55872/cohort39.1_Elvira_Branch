public class Node {
    Node next;   // указатель на следующий элемент
    int data;   // данные

    // Конструктор узла
    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}
